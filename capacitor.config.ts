import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'identificador.paquete',
  appName: 'CloudApp',
  webDir: 'dist/cloud-app'
};

export default config;
